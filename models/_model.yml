version: 2

models:
  - name: eligibility
    config:
      database: "{{ var('tuva_database') }}"
      schema: input_layer
  - name: medical_claim
    config:
      database: "{{ var('tuva_database') }}"
      schema: input_layer
  - name: pharmacy_claim
    config:
      database: "{{ var('tuva_database') }}"
      schema: input_layer
